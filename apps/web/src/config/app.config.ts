import type { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    template: "%s - vesper",
    default: "dashboard - vesper",
  },
  description: "vesper dashboard to manage your databases",
};
