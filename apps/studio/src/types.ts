export type User = {
  email: string;
};

export type Project = {
  cid: string;
  name: string;
  description: string;
  ownerId: string;
  createdAt: string;
  updatedAt: string;
  deletedAt: string;
};
